{
  "name": "wechat2all",
  "version": "0.1.0",
  "description": "Local-first WeChat iLink gateway SDK for bots, agents, and automations",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "echo-bot": "tsx examples/echo-bot.ts",
    "check:examples": "tsc -p examples/tsconfig.json",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "pnpm typecheck && pnpm check:examples && pnpm test && pnpm build"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
